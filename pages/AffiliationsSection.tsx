import React from "react"; import { useTheme } from 'next-themes'; import { useDarkVeil } from '@/components/layout/DarkVeilProvider'; import { useLocation } from 'react-router-dom'; import { useQueryClient } from '@tanstack/react-query'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { useQueryClient } from '@tanstack/react-query'; import SEO from '@/components/SEO'; const AffiliationsSection: React.FC = () => { const { theme } = useTheme(); const { isDarkVeilActive } = useDarkVeil(); const location = useLocation(); const queryClient = useQueryClient(); const isDarkBackground = isDarkVeilActive || theme === 'dark'; const pageSpecificData = queryClient.getQueryCache().entries.find(entry => entry.queryKey[0] === location.pathname); const pageTitle = pageSpecificData?.queryKey[1]?.title || 'Affiliations'; const pageDescription = pageSpecificData?.queryKey[1]?.description || 'My professional affiliations and memberships.'; const pageKeywords = pageSpecificData?.queryKey[1]?.keywords || 'Ernie Cledera, Affiliations, Professional Memberships'; const pageCanonical = pageSpecificData?.queryKey[1]?.canonical || 'https://ernie-cledera.github.io/affiliations'; const pageStructuredData = pageSpecificData?.queryKey[1]?.structuredData || {}; return ( <div className="container mx-auto py-12 px-4"> <Card className={`p-6 ${isDarkBackground ? 'border border-primary/20 backdrop-blur-md' : ''}`}> <CardHeader className="p-0 pb-4"> <CardTitle className="text-2xl font-bold">Affiliations</CardTitle> </CardHeader> <CardContent className="p-0 space-y-6"> {affiliationsData.map((affiliation) => ( <div key={affiliation.id} className="flex flex-col sm:flex-row sm:justify-between sm:items-start text-muted-foreground border-b border-border/30 pb-2 last:border-0 last:pb-0"> <span className="font-semibold text-foreground text-sm md:text-base mb-1 sm:mb-0"> {affiliation.name}</span> <span className="text-sm md:text-base sm:text-right sm:flex-1 sm:ml-4 break-words"> {affiliation.status}</span> <span className="text-sm md:text-base sm:text-right sm:flex-1 sm:ml-4 break-words"> {affiliation.date}</span> </div> <p className="text-sm md:text-base sm:text-right sm:flex-1 sm:ml-4 break-words"> {affiliation.status}</p> </div> </div> </CardContent> </Card> </div> ); }; export default AffiliationsSection;