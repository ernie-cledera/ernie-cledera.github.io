import React from "react"; import { useTheme } from 'next-themes'; import { useDarkVeil } from '@/components/layout/DarkVeilProvider'; import { useLocation } from 'react-router-dom'; import { useQueryClient } from '@tanstack/react-query'; import ProjectCards from '@/components/portfolio/ProjectCards'; import SEO from '@/components/SEO'; const ProjectsPage: React.FC = () => { const { theme } = useTheme(); const { isDarkVeilActive } = useDarkVeil(); const location = useLocation(); const queryClient = useQueryClient(); const isDarkBackground = isDarkVeilActive || theme === 'dark'; const pageSpecificData = queryClient.getQueryCache().entries.find(entry => entry.queryKey[0] === location.pathname); const pageTitle = pageSpecificData?.queryKey[1]?.title || 'My Projects'; const pageDescription = pageSpecificData?.queryKey[1]?.description || 'A collection of my personal and academic projects showcasing my skills in web development, software engineering, and game development.'; const pageKeywords = pageSpecificData?.queryKey[1]?.keywords || 'Ernie Cledera, Projects, Web Development, Software Engineering, Game Development'; const pageCanonical = pageSpecificData?.queryKey[1]?.canonical || 'https://ernie-cledera.github.io/projects'; const pageStructuredData = pageSpecificData?.queryKey[1]?.structuredData || {}; return ( <div className="container mx-auto py-12 px-4"> <h1 className="text-4xl font-bold text-center mb-10">My Projects</h1> <p className="text-center text-muted-foreground mb-10 max-w-2xl mx-auto"> A collection of my personal and academic projects showcasing my skills in web development, software engineering, and game development. </p> <ProjectCards /> </div> ); }; export default ProjectsPage;